version: "3"
services:
    web:
        build: .
        ports:
            - "6000:6000"
        depends_on:
            - sql-server-db
    sql-server-db:
        container_name: sql-server-db
        image: microsoft/mssql-server-linux:2017-latest
        ports:
          - "1433:1433"
        environment:
          SA_PASSWORD: "Mahzan19%"
          ACCEPT_EULA: "Y"